flask>=3.0,<4.0
flask-cors>=4.0
gunicorn>=22.0
requests>=2.32
python-dotenv>=1.0
notion-client>=2.2
anthropic>=0.40
# v1.0.0dg: read use-cases catalog (Ben's separate Django app) from
# its Railway Postgres DB. Binary wheel so Railway doesn't need
# libpq + build tools to deploy.
psycopg[binary]>=3.2
